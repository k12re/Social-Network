/**

The base URL for the API endpoint used to retrieve posts.
@constant {string}
*/
export const apiBase = "https://whatver.com/api/posts";

/**

A URL object representing the API base URL.
@constant {URL}
*/
export const url = new URL(apiBase);
